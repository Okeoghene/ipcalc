subnet_mask = '256.255.255.0' #currently acting as the subnet_mask input

subnet_mask = subnet_mask.split('.') #This converts the input to a list

values = [0, 128, 192, 224, 240, 248, 252, 254, 255]


if subnet_mask == []:
    net_mask = subnet_mask
elif len(subnet_mask) != 4:
    net_mask = subnet_mask
elif subnet_mask[octect] not in values:
    net_mask = subnet_mask


print(net_mask)
